# Docker environment template, rename to .env

# Cluster IP or hostname to ne used for API connections
IP=music.eng.qumulo.com
# This account just needs enough access to connect via API
USER=ccdns
PASSWORD=yourpassword
# The external IP of your docker host that should respond to DNS request
BINDIP=10.120.248.202
# The dns name you wish to respond to
DNSNAMES=ccmusic.eng.qumulo.com altmusic.eng.qumulo.com
# The VLAN ID that your cluster's floating IPs are assigned to
VLANID=0
